= simple_form_for User.new, url: user_session_path do |f|
  = f.input :email, required: false, as: :email
  = f.input :password
  = f.submit t('devise.shared.links.sign_in'), class: 'btn btn-default'
